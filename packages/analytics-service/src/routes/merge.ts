// Stub file - merge routes disabled
// This prevents Railway build errors if file is cached
import type { FastifyInstance } from 'fastify';

export default async function mergeRoutes(app: FastifyInstance) {
  // No routes - this is intentionally empty
  // Merge functionality has been moved to analytics-generator
}

