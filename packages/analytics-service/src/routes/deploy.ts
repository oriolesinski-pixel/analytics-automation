// Stub file - deploy routes disabled
// This prevents Railway build errors if file is cached
import type { FastifyInstance } from 'fastify';

export default async function deployRoutes(app: FastifyInstance) {
  // No routes - this is intentionally empty
  // Deploy functionality has been moved to analytics-generator
}


